<div>
    <span class="block text-xs font-semibold text-gray-500 uppercase tracking-wide mb-1">{{ $label }}</span>
    <span class="text-sm text-gray-900 font-medium">{{ $value ?: '-' }}</span>
</div>
